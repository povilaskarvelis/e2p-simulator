name: packages-ci

on:
  push:
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python package and test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "packages/python[dev,mcp]"

      - name: Run complete Python test suite
        run: |
          export E2P_SHOW_PLOTS=0
          python -m pytest packages/python/tests

  r:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      # Installs Imports/Suggests from packages/r/e2p/DESCRIPTION into the same
      # library R CMD check uses (--as-cran requires suggested packages).
      - name: Install R package dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          working-directory: packages/r/e2p

      - name: R CMD build
        working-directory: packages/r/e2p
        run: R CMD build .

      - name: R CMD check
        working-directory: packages/r/e2p
        run: R CMD check --no-manual --as-cran e2p_*.tar.gz
